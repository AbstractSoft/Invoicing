namespace Invoicing.Domain.Support.Tests.Entity;

using BoundedContexts.Person;
using BoundedContexts.Person.Events;
using Contracts.DomainEvents;
using Xunit;

public class DomainEventTests
{
    [Fact]
    public void Test()
    {
        // var eventDispatcher = new DomainEventDispatcher();
        // eventDispatcher.Subscribe<PersonUpdatedEvent, PersonUpdatedEventHandler>();
        //
        // var personUpdatedEvent = new PersonUpdatedEvent(null);
        // eventDispatcher.Dispatch(personUpdatedEvent);
    }
}